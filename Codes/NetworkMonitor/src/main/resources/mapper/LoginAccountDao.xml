<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD config.mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.dao.LoginAccountDao">
    <select id="findSecurityQuestionByUsernameAndPassword" resultType="SecurityQuestion">
        SELECT security_question_#{questionNum} as question, security_answer_#{questionNum} as answer
        FROM login_accounts
        WHERE username = #{username} AND password = #{password};
    </select>
</mapper>
