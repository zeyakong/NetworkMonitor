<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD config.mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.dao.CardAccountDao">

	<select id="findAllCardAccounts" resultType="CardAccount">
	SELECT *
	FROM card_accounts
	</select>

	<select id="findCardAccountById" resultType="CardAccount">
		SELECT *
		FROM card_accounts
		WHERE account_id = #{id}
	</select>

	<insert id="createCardAccount">
		INSERT INTO card_accounts (account_name, account_address, account_phone, limit, balance)
		VALUES(#{name}, #{address}, #{phone}, #{limit}, #{balance})
	</insert>

	<update id="updateCardAccount">
		UPDATE card_accounts
		SET name = #{name}, account_address = #{address}, account_phone = #{phone}, limit = #{limit}, balance = #{balance}
		WHERE account_id = #{id}
	</update>

	<delete id="deleteCardAccount">
		DELETE FROM card_accounts
		WHERE account_id = #{id}
	</delete>
</mapper>
